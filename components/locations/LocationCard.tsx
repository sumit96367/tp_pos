"use client";
import { motion } from "framer-motion";
import type { PizzaLocation } from "@/types/location";
import { directionsUrl } from "@/lib/directions";
export function LocationCard({ location, onAllLocations }: { location: PizzaLocation; onAllLocations: () => void }) { return <motion.aside className="location-card" aria-live="polite" initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 18 }}><button className="card-close" onClick={onAllLocations} aria-label="Return to all locations">×</button><div className="eyebrow">Tandoori Pizza</div><h2>{location.city}</h2><p className="state">{location.state}</p><p className="address">{location.address}</p><p className={`status ${location.status === "Open" ? "open" : "soon"}`}><span />{location.status === "Open" ? "Now open" : "Coming soon"}</p><div className="card-actions">{location.orderUrl && <a className="order" href={location.orderUrl} target="_blank" rel="noreferrer">Order now</a>}<a className="directions" href={directionsUrl(location)} target="_blank" rel="noreferrer">Directions <span>↗</span></a></div><button className="view-all" onClick={onAllLocations}>View all locations <span>→</span></button></motion.aside>; }
